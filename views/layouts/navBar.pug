nav.navbar.navbar-expand-lg.navbar-light.bg-light.border-bottom
  a.navbar-brand(href='/') Task Manager
  button.navbar-toggler(type='button' data-toggle='collapse' data-target='#navbarSupportedContent' aria-controls='navbarSupportedContent' aria-expanded='false' aria-label='Toggle navigation')
    span.navbar-toggler-icon
  #navbarSupportedContent.collapse.navbar-collapse
    ul.navbar-nav.mr-auto
      li.nav-item.active
        a.nav-link(href=urlFor('users')) All Users

    ul.navbar-nav
      //- li.nav-item
      //-   a.nav-link(href=urlFor('newSession')) Sign In
      //- li.nav-item
      //-   a.nav-link(href=urlFor('newUser')) Sign Up
      if isSignedIn()
        li.nav-item.dropdown
          a#navbarDropdown.nav-link.dropdown-toggle(href='#' role='button' data-toggle='dropdown' aria-haspopup='true' aria-expanded='false')
            | Profile
          .dropdown-menu.dropdown-menu-right(aria-labelledby='navbarDropdown')
            a.dropdown-item(href=urlFor('userProfile')) View Profile
            a.dropdown-item(href=urlFor('editUser')) Edit Profile
            .dropdown-divider
            a.dropdown-item(href=urlFor('sessionDelete') data-method='delete' rel='nofollow') Sign Out
      else
        li.nav-item
          a.nav-link(href=urlFor('newSession')) Sign In
        li.nav-item
          a.nav-link(href=urlFor('newUser')) Sign Up


