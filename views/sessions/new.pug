extends /layouts/application.pug

block content
  .container
    .py-4.text-center
      h2 Login
      p.h6
        | Just fill forms and click 'Submit'

  .container
    +formFor({ url: 'newSession', method: 'post' })
      +input(formObj, 'email')
      +input(formObj, 'password')
      +button('Submit')